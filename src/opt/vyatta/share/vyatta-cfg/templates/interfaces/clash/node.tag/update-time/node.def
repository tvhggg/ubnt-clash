type: u32
help: Config update time, used in cron
default: 86400
syntax:expression: ($VAR(@) >= 30 && $VAR(@) <= 86400) ; "must between 30-86400 seconds"
val_help: u32:30-86400; Config update time in seconds (default 86400)

